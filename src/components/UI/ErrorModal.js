import React from "react";
import Card from "./Card";

const ErrorModal = (props) => {
  return (
    <Card>
        <header>

        </header>

        <div>
            <p>

            </p>
        </div>

        <footer>
            
        </footer>
    </Card>
  );
};

export default ErrorModal;